{
  "scripts": {
    "clean": "rimraf dist/",
    "prestart": "npm run clean",
    "start": "eleventy --serve --quiet",
    "prebuild": "npm run clean",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0",
    "js-yaml": "^3.13.1",
    "rimraf": "^3.0.1"
  },
  "engines": {
    "node": ">=12"
  },
  "private": true
}
