{
  "scripts": {
    "clean": "rimraf dist/",
    "prestart": "npm run clean",
    "start": "eleventy --serve --quiet",
    "prebuild": "npm run clean",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.11.0",
    "js-yaml": "^3.14.0",
    "rimraf": "^3.0.2"
  },
  "engines": {
    "node": ">=12"
  },
  "private": true
}
